# Speaker ID (optional) — install after Coqui venv: pip install -r requirements-voice-id.txt
# Resemblyzer imports pkg_resources (removed in setuptools 82+).
setuptools>=65.0.0,<82
resemblyzer>=0.1.1
librosa>=0.9,<0.11
soundfile>=0.12
# Passive speech gate (optional; falls back to passthrough if missing)
silero-vad>=6.0.0
